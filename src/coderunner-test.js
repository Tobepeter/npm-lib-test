// NOTE: test the vscode code runner

/**
 * 局部运行代码
 *
 * 你可以选中局部的代码，然后按下 `Ctrl+Alt+N` 来运行代码
 * 或者右键点击，选择 `Run Code` 来运行代码
 *
 * 如果不选中，默认是当前文件执行
 */
function test_local_run() {
  console.log('Hello, World')
  console.log('Hello, Coderunner!')
}

function main() {
  test_local_run()
}

main()
